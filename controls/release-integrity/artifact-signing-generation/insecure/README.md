# INSECURE fixture — do not deploy

This isolated fixture intentionally models mutable release identity, broad
long-lived authorization, exportable overprivileged signing keys, fail-open
release behavior, and sensitive evidence retention. It contains only synthetic
data and must never be used as a production signing configuration.
