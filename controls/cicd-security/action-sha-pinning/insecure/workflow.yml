name: Insecure mutable references

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: docker://alpine:latest

  reusable:
    uses: example-org/ci/.github/workflows/reviewed.yml@v1
